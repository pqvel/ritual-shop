import { FC } from "react";
import { Aside } from "../ui/Wrappers";

type FiltersType = {};
// возможно стоит перенести в формы
const AsideFilters: FC = () => {
  return <Aside></Aside>;
};

export default AsideFilters;
